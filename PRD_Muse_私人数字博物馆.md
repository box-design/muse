# PRD：Muse — 私人数字博物馆 App

> **文档版本**：v1.0  
> **产品阶段**：概念定义 & 第一版功能规划  
> **平台**：Android（Kotlin + Jetpack Compose）  
> **日期**：2026-08-23

---

## 一、产品概述

### 1.1 一句话定义

一个把手机相册变成**「私人数字博物馆」**的 Android App。用户不是单纯浏览照片，而是在这里**观看、创作、收藏自己的记忆**。

### 1.2 核心逻辑闭环

整个产品围绕三个动作形成闭环：

```
SEE → CREATE → KEEP → SEE
```

- **SEE（展览）**：发现记忆
- **CREATE（创作）**：创造作品  
- **KEEP（收藏）**：留下作品

收藏的作品可以重新进入展览，形成循环。

### 1.3 产品气质

**Modern Digital Museum × 当代摄影 × 数字档案**

关键词：**现代、克制、艺术、空间感、编辑感、轻科技**

---

## 二、产品定位

### 2.1 定位声明

为**热爱记录生活、注重审美体验、希望赋予照片更多意义**的用户，提供一个不同于传统相册的记忆容器。这里不是存储空间，而是**观看、创作、收藏记忆的场所**。

### 2.2 差异化价值

| 传统相册 App | 私人数字博物馆 |
|-------------|---------------|
| 按时间/地点机械排列 | 自动生成「展览」叙事 |
| 三列 Grid 浏览 | 摄影画册式滚动阅读 |
| 照片加滤镜 | 照片设计成作品（明信片） |
| 收藏 = 喜欢的照片 Grid | 收藏 = 私人数字档案库 |
| 功能堆砌 | 克制、留白、艺术感 |

### 2.3 不做的事（第一版明确排除）

- 不追求功能数量，优先保证核心体验的质感
- 不使用 Glassmorphism（玻璃效果）
- 不使用大量彩色按钮、圆角 Card、渐变背景
- 不做复杂的社交分享（第一版聚焦个人体验）
- 不做胶片模拟/复古风格

---

## 三、目标用户

### 3.1 核心用户画像

**用户 A：生活记录者**
- 经常拍照，手机里有数千张照片
- 很少回看，因为「找不到感觉」
- 希望照片被「组织」成有意义的叙事
- 对设计、排版有一定审美要求

**用户 B：视觉创作者**
- 喜欢摄影、旅行、记录
- 希望把照片变成更有形式感的作品
- 愿意花时间为照片设计排版
- 重视作品的「档案感」和「编号感」

### 3.2 用户痛点

1. **照片越拍越多，越看越麻木** —— 传统相册让照片失去了叙事感
2. **想分享/保存某张照片，但觉得「太普通」** —— 缺乏轻量级的创作工具
3. **收藏的照片散落在各处** —— 没有统一的、有仪式感的收藏空间

---

## 四、信息架构

### 4.1 底部导航（三个 Tab）

```
┌─────────────────────────────────────────┐
│                                         │
│              [ 内容区域 ]                │
│                                         │
├─────────────────────────────────────────┤
│    展览        创作        收藏         │
│  Explore     Create    Collection       │
│                                         │
└─────────────────────────────────────────┘
```

| Tab | 英文名 | 核心动作 | 气质 |
|-----|--------|---------|------|
| 展览 | Explore / Exhibition | 观看、逛自己的记忆 | 沉浸、叙事、空间感 |
| 创作 | Create | 把照片变成作品 | 工具化、极简、设计感 |
| 收藏 | Collection / Archive | 保存真正喜欢的东西 | 冲击力、私人感、档案感 |

### 4.2 全局结构

```
App
├── 展览 (Explore)
│   ├── 展览入口（首页）
│   ├── 展览详情页
│   └── 单张照片全屏页
├── 创作 (Create)
│   ├── 创作首页（入口 + 历史作品）
│   ├── 明信片编辑器
│   └── 调色盘工具
└── 收藏 (Collection)
    ├── 收藏首页（巨大数字 + 单件展示）
    └── 筛选视图（ALL / PHOTOS / POSTCARDS / PALETTES）
```

---

## 五、视觉设计规范

### 5.1 设计语言

**Modern Digital Museum**

依靠**图片 + 排版 + 空间 + 动画**来形成设计，UI 不抢照片的视觉。

### 5.2 色彩系统

第一版极度克制：

| 用途 | 颜色 | 说明 |
|------|------|------|
| 背景色 | 纯黑 / 暖白 | 根据页面气质切换 |
| 文字色 | 灰阶（白→深灰） | 层次分明但不抢眼 |
| 装饰色 | 极少 | 几乎不出现 |
| 照片原色 | 自然呈现 | 照片本身是唯一的彩色来源 |

> 第一版：**黑 / 白 / 灰 + 照片原色**。玻璃、光效、模糊、3D 等视觉效果后续版本再考虑。

### 5.3 字体与排版

- 标题使用大写字母（如 **EXHIBITIONS**、**SUMMER DAYS**）
- 日期格式统一：`2026.06 — 2026.08`
- 数量单位使用 **ARTIFACTS**（如 `128 ARTIFACTS`）
- 档案编号格式：**ARCHIVE 0247**
- 地点 + 日期格式：`TOKYO · 2026.08.12`

### 5.4 通用 UI 原则

- 照片是主角，文字只是帮助理解
- 大量留白，不拥挤
- 避免：彩色按钮、圆角 Card、渐变背景、图标堆叠
- 动画优先使用共享元素转场，而非页面硬切

---

## 六、页面详细设计

---

### 6.1 展览页（Explore / Exhibition）

> **核心目标**：让用户感觉自己在「重新经历记忆」，而非浏览文件。

#### 6.1.1 展览入口（首页）

**布局**：

```
┌────────────────────────────┐
│  EXHIBITIONS               │  ← 顶部标题，极简
│                            │
│  ┌────────────────────┐    │
│  │                    │    │
│  │   [展览封面照片]    │    │  ← 占据屏幕非常大区域
│  │                    │    │     单张巨大照片
│  │                    │    │
│  └────────────────────┘    │
│                            │
│  SUMMER DAYS               │  ← 展览名称
│  2026.06 — 2026.08         │  ← 日期范围
│  128 ARTIFACTS             │  ← 照片数量
│                            │
│  [下一件展览预览...]       │  ← 可上下滑动切换展览
└────────────────────────────┘
```

**设计要点**：
- 顶部标题 **EXHIBITIONS**，字体简洁
- 最大的视觉元素是一张照片，占据屏幕主体
- 照片下方只有少量信息：展览名称、日期范围、照片数量
- 用户上下滑动切换不同展览
- 不要出现「2026年8月 / 123张照片 / 三列 Grid」的传统相册布局

#### 6.1.2 展览的自动生成逻辑

第一版根据以下维度自动生成展览：

| 维度 | 说明 | 示例 |
|------|------|------|
| 拍摄时间 | 连续拍摄的时间段 | 2026.07.12 ~ 07.18 |
| 地点 | 照片携带的地理位置信息 | 上海 |
| 相册结构 | 用户已有的相册分类 | — |

**自动生成示例**：

用户有一批照片：
- 时间：2026.07.12 ~ 07.18
- 地点：上海
- 数量：83 张

App 自动生成展览：

```
SHANGHAI
07.12 — 07.18
83 ARTIFACTS
```

用户可手动修改展览名称。

**未来规划**：更智能的自动策展，如「发现 43 张照片可能来自一次旅行」→ 提示用户 **CREATE EXHIBITION**

#### 6.1.3 进入展览 —— 展览封面

**不要直接跳到照片 Grid。**

用户点击展览后，先出现：

```
┌────────────────────────────┐
│                            │
│  ┌────────────────────┐    │
│  │                    │    │
│  │   [展览封面照片]    │    │  ← 巨大照片
│  │                    │    │
│  │                    │    │
│  └────────────────────┘    │
│                            │
│  SHANGHAI                  │  ← 展览标题
│  2026.07.12 — 07.18        │  ← 日期
│                            │
│      ↑ 向上滑动进入        │  ← 提示
└────────────────────────────┘
```

**交互**：用户向上滑动，进入真正的「摄影展」。

#### 6.1.4 展览内部 —— 摄影画册式浏览

**不是 Grid，而是可以滚动阅读的摄影画册。**

照片不需要全部一样大，布局变化：

```
┌────────────────────────────┐
│                            │
│  ┌────────────────────┐    │
│  │                    │    │  ← 一张大图
│  │   [照片 1]         │    │
│  │                    │    │
│  └────────────────────┘    │
│                            │
│  ┌────────┐  ┌────────┐   │
│  │ [照片2]│  │ [照片3]│   │  ← 两张并列
│  └────────┘  └────────┘   │
│                            │
│        2026.07.14          │  ← 中间出现日期
│        上海 · 外滩          │  ← 偶尔出现地点
│                            │
│  ┌────────────────────┐    │
│  │   [横向大图]       │    │
│  └────────────────────┘    │
│                            │
│  ┌────────┐                │
│  │ [照片5]│                │  ← 一张小图 + 留白
│  └────────┘                │
│                            │
└────────────────────────────┘
```

**布局规则**：
- 一张大图
- 一张小图
- 两张并列
- 一张横向大图
- 中间穿插日期和地点文字
- 整体节奏变化，像翻阅摄影画册

#### 6.1.5 单张照片全屏页

点击照片后进入全屏，UI 极少：

```
┌────────────────────────────┐
│                            │
│                            │
│      [全屏照片]            │  ← 屏幕主体就是照片
│                            │
│                            │
├────────────────────────────┤
│  2026.08.12                │  ← 日期
│  TOKYO                     │  ← 地点
│  23 / 128                  │  ← 当前序号 / 总数
│                            │
│  [收藏]  [制作明信片]      │  ← 底部操作
└────────────────────────────┘
```

**交互**：
- **左右滑动** → 下一张/上一张照片
- **双击** → 收藏
- **长按** → 更多操作（如删除、编辑等）
- 底部操作：**收藏**、**制作明信片**

#### 6.1.6 展览核心动画 —— 共享元素转场

**核心体验**：照片从展览中的位置直接「长成」全屏页面。

```
用户点击展览中的一张照片：
    ↓
照片从原位置平滑扩大到全屏
    ↓
周围其他照片逐渐退到背景/淡出
    ↓
底部信息（日期、地点、序号）淡入
```

**用户感知**：
> 我不是打开了一个新的页面，而是**从展厅里走近了一件作品**。

这是整个 App 最具质感的动画体验，必须优先实现。

---

### 6.2 创作页（Create）

> **核心目标**：让用户感觉自己在「设计一件东西」，而非「给照片加滤镜」。

#### 6.2.1 创作首页

比展览更加工具化，但仍然保持极简。

```
┌────────────────────────────┐
│  CREATE                    │  ← 顶部标题
│                            │
│  ┌────────────────────┐    │
│  │    POSTCARD        │    │  ← 巨大入口 1
│  │    明信片           │    │
│  └────────────────────┘    │
│                            │
│  ┌────────────────────┐    │
│  │    PALETTE         │    │  ← 巨大入口 2
│  │    调色盘           │    │
│  └────────────────────┘    │
│                            │
│  ─────────────────────     │
│  MY CREATIONS              │  ← 历史作品区
│  [作品1] [作品2] [作品3]  │
│                            │
└────────────────────────────┘
```

**入口**：
- **POSTCARD**：明信片创作
- **PALETTE**：调色盘工具
- 下方展示 **MY CREATIONS**（过去做过的作品）

#### 6.2.2 明信片编辑器 —— 核心创作功能

**核心思想**：不是给照片加滤镜，而是把照片设计成一件作品。

**第一步：选择照片**
- 从「展览」中直接进入：**Create Postcard**
- 或在创作 Tab 里选择照片

**第二步：进入编辑器**

```
┌────────────────────────────┐
│                            │
│    [照片画布区域]           │  ← 照片成为整个画布
│                            │
│                            │
├────────────────────────────┤
│  Template  Layout  Text    │  ← 底部工具栏（极简）
│  Info      Color           │
└────────────────────────────┘
```

**底部工具**：
| 工具 | 功能 |
|------|------|
| Template | 选择模板 |
| Layout | 调整布局 |
| Text | 编辑文字 |
| Info | 编辑信息（日期、地点、编号等） |
| Color | 调整颜色 |

**第三步：模板系统**

第一版提供 **6～8 个模板**：

| 模板名 | 风格描述 |
|--------|---------|
| Classic | 非常干净，极简 |
| Editorial | 像现代摄影杂志 |
| Museum | 像博物馆藏品标签 |
| Travel | 强调地点和日期 |
| Minimal | 几乎没有装饰 |
| Archive | 强调编号、时间、metadata |
| Date | 突出拍摄时间 |
| Postcard | 保留传统明信片结构，但做成现代版本 |

**模板控制的内容**：
- 图片比例
- 图片位置
- 留白大小
- 标题样式与位置
- 日期格式与位置
- 地点显示
- 编号（如 ARCHIVE 0247）
- 字体选择
- 信息层级

**排版示例**：

```
┌────────────────────────────┐
│                            │
│      [照片区域]            │
│                            │
│                            │
│  ─────────────────────     │
│                            │
│  TOKYO                     │  ← 标题
│  2026.08.12                │  ← 日期
│  ARCHIVE 0247              │  ← 编号
│                            │
└────────────────────────────┘
```

**第四步：照片裁切**

提供三种模式：
| 模式 | 说明 |
|------|------|
| Fit | 完整显示照片，可能留黑边 |
| Fill | 填满画布，可能裁切部分照片 |
| Crop | 用户手动拖动调整裁切区域 |

用户拖动照片时**实时看到最终效果**。

**第五步：自动生成**

用户选照片后，点击 **GENERATE**：

App 自动生成多个不同版本：
- 不同照片裁切方式
- 不同排版布局
- 不同文字位置
- 不同模板风格

用户只需要**挑一个自己喜欢的**。

#### 6.2.3 调色盘工具（Palette）

**核心功能**：从照片中提取色彩，形成调色盘。

**流程**：

1. 选择照片
2. App **自动分析**照片，提取 **3～6 个主要颜色**
3. 形成漂亮的色彩组合

**提取示例**：

```
┌────────────────────────────┐
│                            │
│      [照片预览]            │
│                            │
├────────────────────────────┤
│                            │
│  ┌────┐ ┌────┐ ┌────┐    │
│  │#D9E│ │#879│ │#D1A│    │  ← 提取的颜色块
│  │1E3 │ │A9C │ │77A │    │
│  └────┘ └────┘ └────┘    │
│                            │
│  ┌────┐ ┌────┐            │
│  │#3B5│ │#...│            │
│  │054 │ │     │            │
│  └────┘ └────┘            │
│                            │
│  [保存调色盘]              │
│  [应用到明信片]            │
│  [找相似照片]              │  ← 未来功能
└────────────────────────────┘
```

**调色盘可以做什么**：

| 功能 | 说明 |
|------|------|
| 保存调色盘 | 命名保存，如 **TOKYO NIGHT** |
| 修改颜色 | 用户拖动色彩参数微调 |
| 应用到明信片 | 明信片自动采用这组颜色 |
| 找相似照片 | 未来功能：显示和该色盘相似的照片 |

---

### 6.3 收藏页（Collection / Archive）

> **核心目标**：让用户感觉自己在「拥有自己的作品档案」，而非「标记喜欢的照片」。

#### 6.3.1 收藏首页 —— Digital Archive

**三个 Tab 中最有风格、最有冲击力的页面。**

```
┌────────────────────────────┐
│                            │
│         128                │  ← 非常巨大的数字
│      COLLECTED             │  ← 状态文字
│                            │
│  ┌────────────────────┐    │
│  │                    │    │
│  │   [收藏作品大图]    │    │  ← 单件巨大作品
│  │                    │    │     一屏只展示一件
│  │                    │    │
│  └────────────────────┘    │
│                            │
│  ARCHIVE 0247              │  ← 编号
│  TOKYO · 2026.08.12        │  ← 地点 + 日期
│                            │
│      ← 滑动 →              │  ← 左右切换
└────────────────────────────┘
```

**设计要点**：
- 打开第一眼就是一个**非常巨大的数字**（如 **128**）+ **COLLECTED**
- 下面是一件**非常大的收藏作品**（原始照片 / 明信片 / 调色盘）
- 用户**左右滑动**切换作品
- 每件收藏有自己的**编号**（如 ARCHIVE 0247）
- 一屏只展示一件作品，强调「私人藏品」的感觉

**滑动动画**：
- 上一件作品缩小并离开
- 下一件作品进入
- 编号、日期、地点跟着作品一起变化

**整体感觉**：翻阅自己的私人艺术收藏。

#### 6.3.2 收藏分类筛选

顶部有极简筛选：

```
ALL    PHOTOS    POSTCARDS    PALETTES
```

| 筛选项 | 内容 |
|--------|------|
| ALL | 所有收藏 |
| PHOTOS | 原始照片 |
| POSTCARDS | 创作的明信片 |
| PALETTES | 保存的调色盘 |

收藏的东西**不只是照片**，而是用户在 App 里创造和保存的一切。

---

## 七、动画设计标准

> **设计目标**：对标 iOS 动画质感，建立物理真实感、空间连续性与手势可逆性。  
> **核心原则**：每个动画都在回答三个问题——**这是哪？从哪来？到哪去？**

### 7.1 动画哲学

| iOS 核心原则 | Muse 中的体现 | 第一版要求 |
|-------------|--------------|-----------|
| **Spring Physics** 弹簧物理 | 所有位移/缩放使用弹簧曲线，拒绝线性插值 | 强制 |
| **Continuity** 连续性 | 元素在页面间「活着」，不会突然消失再出现 | 强制 |
| **Gesture-Driven** 手势驱动 | 动画可被手指打断、反转、释放后继续 | 强制 |
| **Layered Depth** 层次深度 | 通过视差、缩放建立空间 Z 轴 | 推荐 |
| **Purposeful Motion** 有目的的动效 | 每个动画解释空间关系与状态变化 | 强制 |

### 7.2 全局动画参数规范

所有动画统一使用以下物理参数，确保全局手感一致：

| 参数 | 值 | 适用场景 |
|------|-----|---------|
| 阻尼比 (Damping Ratio) | 0.75 | 通用弹簧动画 |
| 刚度 (Stiffness) | 300 | 通用弹簧动画 |
| 质量 (Mass) | 1.0 | 通用弹簧动画 |
| 标准持续时间 | 350–450ms | 页面转场、元素出现 |
| 快速反馈 | 150–200ms | 按钮按压、图标切换 |
| 延迟层级 | 80–120ms | 信息层跟随主元素出现 |
| 手势中断阈值 | 0.3 | 滑动超过 30% 即完成转场，否则回弹 |

> **实现方式**：Jetpack Compose `spring(dampingRatio = 0.75f, stiffness = 300f)`，禁止 `tween()` 用于位移/缩放。

### 7.3 场景级动画 Spec

#### 7.3.1 共享元素转场（展览 → 全屏照片）

**优先级：P0（产品灵魂）**

**触发**：用户在展览中点击一张照片。

**正向动画（进入全屏）**：

| 元素 | 起始状态 | 结束状态 | 曲线 | 时长 | 延迟 |
|------|---------|---------|------|------|------|
| 目标照片 | 列表中的位置/尺寸 | 全屏居中，fit 屏幕 | Spring, 阻尼 0.75 | 400ms | 0ms |
| 同列上下照片 | 原位 | 向两侧推开 40%，透明度 0.3 | Spring, 阻尼 0.8 | 400ms | 0ms |
| 其他区域照片 | 原位 | 透明度 0，轻微模糊（可选） | easeOut | 250ms | 50ms |
| 底部信息栏 | 屏幕外（y = +60dp） | 屏幕内（y = 0） | Spring, 阻尼 0.7 | 350ms | 100ms |
| 状态栏/导航栏 | 显示 | 隐藏（沉浸式） | easeOut | 200ms | 0ms |

**反向动画（返回展览）**：
- 与正向完全对称，照片从全屏缩回列表中的原始位置
- 周围照片从推开状态弹簧回弹到原位
- 信息栏向下滑出

**手势可逆性**：
- 动画播放过程中，用户可向下滑动打断
- 照片跟随手指 Y 轴位移并同步缩小
- 释放时：位移超过屏幕高度 30% → 完成 dismiss，弹簧回弹到展览页；未超过 → 弹簧回弹到全屏

**参考**：iOS Photos App 打开照片动画，但节奏放慢 15%，增强博物馆仪式感。

---

#### 7.3.2 全屏照片内交互

**左右滑动切换（下一张/上一张）**：

| 元素 | 手势响应 | 动画参数 |
|------|---------|---------|
| 当前照片 | 跟随手指水平位移，透明度从 1.0 降到 0.6 | 手势驱动，1:1 映射 |
| 新照片 | 从屏幕边缘跟随手指进入，透明度从 0.6 升到 1.0 | 手势驱动，1:1 映射 |
| 底部信息 | 旧信息向左/右滑出，新信息从反方向滑入 | Spring, 阻尼 0.75, 200ms |
| 释放行为 | 位移超过 25% → 弹簧完成切换；未超过 → 弹簧回弹 | Spring, 阻尼 0.75 |

**向下滑动返回（Dismiss）**：

| 阶段 | 行为 |
|------|------|
| 拖动 | 照片跟随手指 Y 轴位移，同步按比例缩小（scale = 1 - 0.3 * progress），背景逐渐变黑（alpha = 0.3 + 0.7 * progress） |
| 释放判断 | 位移超过屏幕高度 33% 或速度 > 800dp/s → 完成 dismiss |
| 完成 | 照片缩回原列表位置，弹簧回弹，展览页恢复 |
| 取消 | 照片弹簧回弹到全屏，背景恢复透明 |

**双击收藏**：

| 元素 | 动画 | 参数 |
|------|------|------|
| 心形图标 | 从 scale 0 → 1.2（过冲）→ 1.0（回弹） | Spring, 阻尼 0.6, 300ms |
| 照片边缘 | 轻微脉冲（scale 1.0 → 1.02 → 1.0） | Spring, 阻尼 0.8, 200ms |

---

#### 7.3.3 展览入口 —— 展览切换

**触发**：用户在展览入口上下滑动切换展览。

| 元素 | 行为 | 参数 |
|------|------|------|
| 当前展览照片 | 跟随手指垂直位移，同步缩放（1.0 → 0.85 当离开中心） | 手势驱动 |
| 目标展览照片 | 从屏幕边缘进入，同步缩放（0.85 → 1.0） | 手势驱动 |
| 文字信息层 | 与照片同步位移，但速度系数 1.0（无视差） | 手势驱动 |
| 数字（ARTIFACTS） | 旧数字向上滑出淡出，新数字从下方滑入 | Spring, 阻尼 0.7, 250ms, 延迟 80ms |
| 释放吸附 | 照片弹簧吸附到居中位置 | Spring, 阻尼 0.75, 300ms |

---

#### 7.3.4 展览封面 → 展览内部

**触发**：用户在展览封面向上滑动。

| 阶段 | 元素 | 行为 | 参数 |
|------|------|------|------|
| 手势驱动 | 封面照片 | 放大并向上移动，逐渐融入展览内部第一个布局 | 手势驱动，1:1 映射 |
| 手势驱动 | 展览标题 | 向上移动并缩小，变成展览内部的第一个文字标签 | 手势驱动 |
| 手势驱动 | 背景 | 从封面纯色过渡到展览内部的画册背景 | 手势驱动 |
| 释放判断 | — | 滑动超过 40% → 弹簧完成展开；未超过 → 弹簧回弹到封面 | Spring, 阻尼 0.75 |

> **设计意图**：这不是页面跳转，而是「揭开展览幕布」的连续体验。

---

#### 7.3.5 展览内部滚动 —— 照片入场

**触发**：用户滚动展览内部，照片进入视口。

| 元素 | 行为 | 参数 |
|------|------|------|
| 照片 | 从 scale 0.96 + opacity 0.8 动画到 scale 1.0 + opacity 1.0 | Spring, 阻尼 0.8, 300ms |
| 文字标签 | 从左侧滑入（x = -20dp → 0） | Spring, 阻尼 0.75, 250ms, 延迟 100ms |
| 横向大图 | 内部视差：图片内容轻微反向移动（0.1x 滚动速度） | 持续 |

**滚动物理**：
- 惯性滚动：iOS 风格，有速度衰减
- 边缘阻尼：到达顶部/底部时，滚动阻力增加（over-scroll 效果）
- 使用 Compose `LazyColumn` + 自定义 `FlingBehavior`

---

#### 7.3.6 收藏页 —— 藏品滑动切换

**优先级：P1（差异化体验）**

**触发**：用户在收藏页左右滑动切换藏品。

| 元素 | 起始状态 | 结束状态 | 曲线 | 时长 | 延迟 |
|------|---------|---------|------|------|------|
| 当前作品 | 居中，scale 1.0，opacity 1.0 | 向滑动方向位移 50%，scale 0.85，opacity 0.4 | 手势驱动 | — | 0ms |
| 新作品 | 从反方向屏幕外进入，scale 0.85，opacity 0.4 | 居中，scale 1.0，opacity 1.0 | 手势驱动 | — | 0ms |
| 编号（ARCHIVE XXXX） | 旧编号垂直向上滑出（y = 0 → -20dp），opacity 1.0 → 0.0 | 新编号从下方滑入（y = +20dp → 0），opacity 0.0 → 1.0 | Spring, 阻尼 0.7 | 250ms | 50ms |
| 日期/地点 | 同上 | 同上 | Spring, 阻尼 0.7 | 250ms | 80ms |
| 巨大数字 | 旧数字滚动翻转（像机械计数器） | 新数字滚动翻转到位 | Spring, 阻尼 0.8 | 400ms | 0ms |
| 释放吸附 | — | 作品弹簧吸附居中 | Spring, 阻尼 0.75 | 300ms | — |

> **设计意图**：像翻阅私人档案柜的抽屉，比 iOS 默认更「紧」的弹簧感。

---

#### 7.3.7 底部 Tab 切换

| 元素 | 行为 | 参数 |
|------|------|------|
| 选中图标 | scale 1.0 → 1.15（过冲）→ 1.0（回弹） | Spring, 阻尼 0.6, 300ms |
| 未选中图标 | 颜色从激活色渐变为未激活色 | easeOut, 200ms |
| 内容区域 | 交叉淡入淡出（Cross-fade） | easeInOut, 200ms |
| 选中指示条（如有） | 水平弹簧移动到新 Tab 位置 | Spring, 阻尼 0.75, 300ms |

---

#### 7.3.8 明信片编辑器 —— 模板切换

| 元素 | 行为 | 参数 |
|------|------|------|
| 当前模板 | scale 1.0 → 0.9，opacity 1.0 → 0.0 | easeOut, 150ms |
| 新模板 | scale 0.9 → 1.0，opacity 0.0 → 1.0 | Spring, 阻尼 0.75, 250ms |
| 文字元素 | 根据新模板布局，从旧位置弹簧移动到新位置 | Spring, 阻尼 0.8, 300ms, 延迟 50ms |

**键盘弹出**：
- 画布区域向上压缩，使用弹簧曲线
- 被遮挡元素自动上移，保持可见

**色块按压**：
- 按下：scale 1.0 → 0.95，duration 100ms
- 释放：scale 0.95 → 1.0，Spring 回弹

---

#### 7.3.9 数字滚动动画

**适用场景**：收藏页的巨大数字变化、编号变化。

| 元素 | 行为 | 参数 |
|------|------|------|
| 旧数字 | 垂直向上滑出（y = 0 → -30dp），clip 在容器内 | easeIn, 200ms |
| 新数字 | 从下方滑入（y = +30dp → 0），clip 在容器内 | Spring, 阻尼 0.7, 300ms |
| 容器 | 固定高度，overflow clip | — |

> **实现方式**：Compose `AnimatedContent` + `slideInVertically` / `slideOutVertically`。

### 7.4 技术实现方案

| 技术点 | 方案 | 备注 |
|--------|------|------|
| 弹簧动画 | `androidx.compose.animation.core.spring()` | 全局统一参数 |
| 共享元素转场 | 自定义 `Modifier` + `Animatable` 计算位置差值 | 需精确测量列表中照片的 bounds |
| 手势驱动动画 | `GestureDetector` + `Animatable` 联动 | 支持中断与反转 |
| 页面转场 | `AnimatedContent` + `with` 作用域 | 配合 `Transition` 管理多元素 |
| 滚动物理 | `LazyColumn` + 自定义 `FlingBehavior` | 参考 iOS `UIScrollView` 衰减曲线 |
| 数字滚动 | `AnimatedContent` + `slideIn/slideOut` | 配合 `clip` 实现机械计数器感 |
| 层次管理 | `Modifier.zIndex()` | 确保动画过程中图层顺序正确 |

### 7.5 性能保障

- **60fps 目标**：动画期间暂停非必要计算（如照片分析、网络请求）
- **渐进加载**：动画开始前，目标照片必须已加载低分辨率缩略图；高清图在动画完成后加载
- **预加载**：收藏页预加载相邻 2 件作品；展览内部预加载视口外 3 屏内容
- **避免 GC**：使用 `remember` + `derivedStateOf` 缓存计算结果，避免动画帧内分配对象
- **GPU 层**：复杂动画元素使用 `Modifier.graphicsLayer { }` 开启硬件加速

### 7.6 第一版动画优先级

| 优先级 | 动画 | 原因 |
|--------|------|------|
| **P0** | 共享元素转场（展览 → 全屏） | 产品灵魂，没有它就没有 Muse |
| **P0** | 全屏照片手势（向下滑返回 + 左右滑动切换） | 基础体验，必须手势可逆 |
| **P1** | 收藏页滑动切换 | 差异化体验，三个 Tab 中最有冲击力的动画 |
| **P1** | 展览内部滚动入场 | 质感保障 |
| **P1** | 底部 Tab 切换 | 基础 |
| **P2** | 展览封面 → 展览内部手势展开 | 锦上添花 |
| **P2** | 数字滚动动画 | 细节质感 |
| **P2** | 视差效果 | 第二版再细化 |
| **P3** | 模糊背景、粒子效果 | 后续版本 |

---

## 八、交互设计

### 8.1 全局手势

| 手势 | 场景 | 动作 |
|------|------|------|
| 点击 | 展览中的照片 | 进入全屏（共享元素转场） |
| 左右滑动 | 全屏照片页 | 切换上一张/下一张 |
| 左右滑动 | 收藏页 | 切换上一件/下一件藏品 |
| 上下滑动 | 展览入口 | 切换不同展览 |
| 向上滑动 | 展览封面 | 进入展览内部 |
| 双击 | 全屏照片 | 收藏/取消收藏 |
| 长按 | 全屏照片 | 弹出更多操作 |

### 8.2 核心转场动画

#### 8.2.1 共享元素转场（展览 → 全屏照片）

**优先级：高**

```
触发：用户在展览中点击一张照片
    ↓
1. 照片从当前位置/大小开始动画
2. 平滑扩大到全屏尺寸
3. 周围其他照片淡出/退到背景
4. 底部信息（日期、地点、序号）淡入
5. 返回时反向动画
```

**技术要求**：使用 Jetpack Compose 的 `SharedElementTransition` 或自定义动画实现。

#### 8.2.2 收藏页滑动切换

```
触发：用户在收藏页左右滑动
    ↓
1. 当前作品向滑动方向缩小并移出
2. 编号、日期、地点同步淡出
3. 新作品从反方向进入并放大
4. 新编号、日期、地点同步淡入
```

---

## 九、数据与内容

### 9.1 展览自动生成规则（第一版）

**输入**：用户手机相册中的照片（含时间、地点、相册等 metadata）

**规则**：

1. **时间聚类**：连续拍摄超过 N 张（如 20 张）且时间跨度在 M 天内（如 7 天）的照片组成一个候选集
2. **地点聚类**：同一地点（精度约城市级别）的照片优先归入同一展览
3. **相册继承**：用户已有的相册分类作为展览名称的候选
4. **命名**：默认使用地点名或时间范围，用户可修改

**输出**：一个展览对象，包含：
- 展览名称
- 封面照片（候选集中最具代表性的一张）
- 日期范围
- 照片列表
- 照片数量

### 9.2 编号系统

收藏作品使用统一编号：

```
ARCHIVE {4位数字}
```

示例：`ARCHIVE 0247`、`ARCHIVE 0001`

编号按收藏时间顺序递增，不可重复。

### 9.3 明信片数据结构

```
Postcard {
  id: String
  sourcePhotoId: String        // 源照片ID
  template: String             // 模板名称
  layout: LayoutConfig         // 布局配置
  textElements: [TextElement]  // 文字元素列表
  colorScheme: [Color]         // 配色方案
  cropMode: Fit | Fill | Crop  // 裁切模式
  cropOffset: Offset           // 裁切偏移（手动模式）
  createdAt: Date
  updatedAt: Date
}
```

### 9.4 调色盘数据结构

```
Palette {
  id: String
  name: String                 // 如 "TOKYO NIGHT"
  sourcePhotoId: String        // 源照片ID
  colors: [Color]              // 3~6 个颜色
  createdAt: Date
}
```

---

## 十、技术建议

### 10.1 技术栈

| 层级 | 建议技术 |
|------|---------|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose |
| 动画 | Compose Animation API + Shared Element Transition |
| 图片加载 | Coil |
| 本地存储 | Room（数据库）+ DataStore（偏好设置） |
| 照片读取 | MediaStore API |
| 颜色提取 | Android Palette API 或自定义算法 |

### 10.2 关键技术点

1. **共享元素转场**：需要精确计算照片在列表中的位置和尺寸，与全屏位置的差值，使用 `Animatable` 实现平滑过渡
2. **摄影画册式布局**：使用 Compose 的 `LazyColumn` + 自定义 `Layout`，根据照片比例和位置规则动态计算每张照片的尺寸
3. **颜色提取**：使用 Android 的 `Palette` 类或自定义 K-Means 聚类算法从 Bitmap 提取主色调
4. **照片裁切实时预览**：使用 `Canvas` 绘制照片，根据用户手势实时计算 `scale` 和 `offset`

### 10.3 性能考虑

- 展览封面和内部照片使用**渐进式加载**（先显示低分辨率缩略图，再加载高清图）
- 收藏页滑动切换时**预加载**相邻作品
- 照片分析（颜色提取）在**后台线程**完成，避免阻塞 UI

---

## 十一、版本规划

### 11.1 第一版（MVP）—— 核心闭环

**目标**：实现 SEE → CREATE → KEEP 的基础闭环，验证核心体验。

**必须包含**：

| 模块 | 功能 |
|------|------|
| 展览 | 展览入口（巨大封面 + 信息）、展览自动生成（时间+地点）、展览内部（画册式滚动）、单张全屏、共享元素转场 |
| 创作 | 明信片编辑器（8个模板 + 裁切 + 文字编辑）、调色盘（自动提取 + 保存）、自动生成多版本 |
| 收藏 | 巨大数字 + 单件展示 + 编号系统、左右滑动切换、ALL/PHOTOS/POSTCARDS/PALETTES 筛选 |

**第一版明确不做**：
- 社交分享
- 云同步
- AI 智能策展（只做规则自动生成）
- 玻璃效果 / 3D / 复杂视觉特效
- 视频支持
- 多设备同步
---

### 11.2 体验指标

- 共享元素转场动画的流畅度（帧率 > 55fps）
- 展览内部滚动的流畅度
- 收藏页滑动切换的响应速度（< 100ms）

---

## 十二、附录

### 12.1 术语表

| 术语 | 定义 |
|------|------|
| Exhibition | 展览，由一组相关照片自动或手动组织成的叙事单元 |
| Artifact | 展品，指单张照片或创作作品 |
| Postcard | 明信片，通过模板和排版将照片设计成的作品 |
| Palette | 调色盘，从照片中提取的色彩组合 |
| Archive | 档案，收藏的作品集合，每件有唯一编号 |
| Shared Element Transition | 共享元素转场，一种动画效果，让元素在不同页面间平滑过渡 |

### 12.2 参考与灵感

- 当代摄影画册排版（如 Aperture、Steidl 出版社风格）
- 博物馆藏品标签设计（如 MoMA、Tate Modern）
- 现代编辑设计（如 Kinfolk、Cereal 杂志）
- 数字档案系统（如 Are.na、Pinterest 的早期极简风格）

---

> **结语**：这个 App 最重要的不是功能数量，而是三个体验——看照片时感觉自己在重新经历记忆、做明信片时感觉自己在设计一件东西、收藏时感觉自己在拥有自己的作品档案。如果这三个感觉成立，即使第一版功能很少，也会比功能很多的普通相册更有产品辨识度。